# יומן שינויים — MUNI 2026

הפורמט מבוסס על [Keep a Changelog](https://keepachangelog.com/he/1.1.0/),
והפרויקט עוקב אחר [Semantic Versioning](https://semver.org/lang/he/).

---

## [Unreleased]

### השתנה 🔧 — סשן 2026-06-21 (אייקון "חניה" — קישור חיצוני ל-MetroPark)
- אייקון **חניה** בשורת השירותים העליונה הפך לקישור חיצוני: `https://www.metropark.co.il/select-authority` (שירות חניה ארצי).
- הוספת `href` בלבד — האייקון `parking.svg` לא שונה.

### נוסף ✨ — סשן 2026-06-21 (אייקון "פורטל ספקים" + ניווט חיצוני)
- נוצר `/icons/suppliers-portal.svg` חדש בסגנון תואם לסט:
  - תיק עסקי (briefcase) עם ידית מקושתת + פס מנתק טורקיז במרכז + מנעול לבן
  - מסמך/חוזה מציץ מהתיק (3 שורות טקסט)
  - תג checkmark ירוק-טורקיז בפינה תחתונה-ימנית = "ספק מאושר"
  - צבעים: outline `#027DB3`, מילויים `#83DFE8` + `#5CEBBE`
- נוסף ל-`Home.jsx` services array — בין "פורטל ילדים" ל"טפסים דיגיטליים" (קבוצת פורטלים חיצוניים סמוכה). שורת השירותים מציגה כעת 10 פריטים.
- `href` חיצוני ל-`https://eprvendorst.eprmuni.co.il/login?muniCode=2`.

### מתוכנן
- חיבור ל-API של ה-backend
- מערכת אימות אמיתית
- שילוב מערכת תשלום מאובטחת
- גרסת מובייל ייעודית
- תרגום לערבית
- Dark mode
- חיבור כפתורי "המשך לרכישה" ב-`/shop` לעמודי תשלום אמיתיים
- ניווט פרטני בכרטיסי MenuCard לחלקים הספציפיים ב-`/my-account` / `/payments` / `/forms` (כעת כל פריט מנווט לעמוד אך לא לסעיף פנימי)

---

## [0.2.0] — 2026-06-21

> תקופה: 2026-06-19 → 2026-06-21
> Deployment: https://muni2026v1.choreshchana.workers.dev (Cloudflare Workers)
> תכולה: ליטוש עיצובי מקיף לפי Figma + הוספת זרימת "רוכשים בקליק" (אייקון + עמוד) + תיקוני RTL וניווט.

### תוקן 🐛 — סשן 2026-06-21 (תיקון משלים — "שילוט" חתוך + סמליות חצים סופית)
- **"שילוט" לא נראה** בעמדת start: `md:justify-center` על שורת השירותים גרם לפריטים להתרכז במרכז ה-container, ועם 9 פריטים שחורגים מ-1392px (תוכן רוחב max-w-1440 פחות padding) הפריט הראשון ("שילוט") נחתך בצד הימני. הוסר `md:justify-center` — נשאר רק `justify-start`, כך שהפריטים תמיד מצמדים לימין (start ב-RTL) ו"שילוט" תמיד נראה כראשון.
- **סמליות החצים סופית — convention עברי**: הוחזרו הסימנים לקונבנציה הסטנדרטית בעברית RTL:
  - `ChevronRight` (חץ ימינה ויזואלית) → "הקודם" (`scroll(+1)`, toward start, RIGHT)
  - `ChevronLeft` (חץ שמאלה ויזואלית) → "הבא" (`scroll(-1)`, toward end, LEFT)
  - עם `justify-start` קבוע, בעמדה התחלתית "שילוט" כבר נראה — אז חוסר תזוזה ב-`ChevronRight` בעמדת start הוא תקין (אין אחורה ללכת).

### תוקן 🐛 — סשן 2026-06-21 (חצי גלילה ב-RTL + הוספת ניווט לפריטי MenuCards)
- **חץ ימינה לא עבד**: בשורת השירותים העליונה, `ChevronRight` היה מקושר ל-`scroll(1)` (positive scrollLeft) — ב-Chrome RTL מודרני זה מנסה לגלול לכיוון `start`, אבל בעמדה התחלתית (scrollLeft=0) זה לא מזיז. הוחלפו הסימנים:
  - `ChevronRight` → `scroll(-1)` (gradient toward `end` / "הבא"), aria-label "הבא"
  - `ChevronLeft` → `scroll(1)` (toward `start` / "הקודם"), aria-label "הקודם"
  - עכשיו לחיצה על כל אחד מהחצים תמיד מזיזה את הקרוסלה ב-direction שמתאים לסמליות החץ (LTR-style: ימינה = הבא).
- **פריטי MenuCards ב-Home לא היו לחיצים**: 4 הכרטיסים בשורה התחתונה (האזור האישי / פעולות מהירות / תשלומים וגבייה / אישורים וטפסים) הציגו פריטי טקסט בלבד. נוספו `to`-routes לכל פריט שאינו expandable:
  - **האזור האישי שלי** → `/my-account` (כל הפריטים)
  - **פעולות מהירות** → רוכשים בקליק→`/shop`, פריטי טופס→`/forms`, פריטי תשלום→`/payments`
  - **תשלומים וגבייה** → `/payments` (כל הפריטים)
  - **אישורים וטפסים** → `/forms` (כל הפריטים)
  - פריטים עם `children` (הנכס שלי, חינוך, הנדסה) נשארו expandable כמקודם.

### השתנה 🔧 — סשן 2026-06-21 (Shop — תוכן ועיצוב לפי קובץ Excel "רוכשים בקליק.xlsx")
- הוחלף מערך המוצרים ב-`Shop.jsx` ל-7 קטגוריות מדויקות מהאקסל של המשתמש:
  1. **מיסים, קנסות ואגרות** — ארנונה, דוחות חניה, אגרות שילוט (🧾, amber/yellow)
  2. **חינוך, צהרונים וחוגים** — תשלומי הורים, הרשמה לגנים, חוגי מתנ"ס וקורסים (🎓, sky/blue)
  3. **תרבות, אירועים ופנאי** — כרטיסים להופעות, אירועי קיץ ופעילויות עירוניות (🎭, rose/pink)
  4. **רישוי עסקים, הנדסה ומכרזים** — היתרי בניה, חוברות מכרז, רישיונות עסק (🏗️, stone/zinc)
  5. **חנות ושירותים לתושב** — פחי אשפה, רישיון כלבת/וטרינריה, תווי חניה (🛍️, emerald/green)
  6. **שירותי דת וקהילה** — רישום נישואין, אגרות מקווה, אגרות קבורה (🕊️, violet/purple)
  7. **רווחה ובריאות** — מרכזי יום, טיפת חלב, שירותים סוציאליים (🤲, red/rose)
- סגנון "Cutesy 3D Model" לפי האקסל — אמוג'י גדול 120px, gradient רך לכל קטגוריה, `drop-shadow` כפול (12px+4px) לעומק תלת-ממדי, hover עם `scale-110` ו-translate-up.
- הוסר ה-CommunityCard ("תרומה לקהילה") — לא הופיע באקסל. תיאור Hero עודכן ל"כל התשלומים והרכישות במקום אחד".
- חבילות `tags` ו-import של Lucide icons הוסרו מ-`Shop.jsx` (לא בשימוש כעת).

### השתנה 🔧 — סשן 2026-06-21 (תווית האייקון "חינוך" → "פורטל ילדים")
- בשורת השירותים העליונה (`Home.jsx`) — האייקון המקושר ל-`https://departmentof.choreshchana.workers.dev/` שונה מ-**"חינוך"** ל-**"פורטל ילדים"** (משקף טוב יותר את אופי אתר היעד).
- שאר ההופעות של "חינוך" במערכת (פריט בכרטיס "פעולות מהירות", Footer, Forms, ServiceCategory, Shop) נשארו ללא שינוי — הן מתייחסות לקטגוריות חינוך כלליות ולא לפורטל הילדים הספציפי.

### נוסף ✨ — סשן 2026-06-21 (דף "רוכשים בקליק" — חנות התושב הדיגיטלית)
- נוסף עמוד חדש **`/shop`** (`src/pages/Shop.jsx`) — חנות תושב לפי 222.jpg:
  - Hero כותרת "רוכשים בקליק" עם תיאור קצר
  - Grid של 6 כרטיסי מוצרים (responsive: 1/2/3 עמודות): תוצרת מקומית, בתי עסק ברשות, מבצעים וחיסכון, תרבות ומורשת, קניות יומיומיות, חוגים וקייטנות
  - כל כרטיס: header צבעוני עם gradient + אייקון Lucide גדול, כותרת, תיאור, chips של תגיות, כפתור CTA "המשך לרכישה"
  - כרטיס תרומה לקהילה רחב בתחתית — gradient כחול-מוני עם אייקון לב
- ניווט: לחיצה על **"רוכשים בקליק"** באייקון שבשורת השירותים העליונה, וגם בפריט שבכרטיס "פעולות מהירות" — מובילה ל-`/shop`
- `App.jsx` — נוסף `Route path="/shop"` ל-React Router
- `Home.jsx` — services array תומך כעת ב-`to` (Router Link פנימי) בנוסף ל-`href` (external)
- `MenuCard.jsx` — `MenuItem` תומך ב-`to` ל-Router navigation

### השתנה 🔧 — סשן 2026-06-21 (אייקון "רוכשים בקליק" עודכן ל-Mobile Pay)
- הוחלף האייקון של **"רוכשים בקליק"** מ-`online-shop.svg` (חזית חנות) ל-`mobile-pay.svg` חדש — מבטא תשלום מובייל בסגנון Apple Pay / Bit / Google Pay:
  - סמארטפון אנכי (מסגרת + מסך + רמקול + home indicator)
  - כרטיס אשראי במסך (chip + פסי מספר)
  - 3 גלי NFC מצד ימין — אינדיקציית tap-to-pay / contactless
  - צבעים תואמים לסט: outline `#027DB3`, מילויים `#83DFE8` + `#5CEBBE`
- `online-shop.svg` הוסר מהפרויקט (לא היה בשימוש לאחר ההחלפה; ניתן לשחזר מ-git history אם יידרש).

### השתנה 🔧 — סשן 2026-06-21 (סדר חדש בשורת השירותים — הסרת "חוגים")
- הוסר הפריט **"חוגים"** (`classes.svg`) משורת השירותים העליונה.
- הפריט **"רוכשים בקליק"** הועבר ממיקום ראשון למיקום שהיה של "חוגים" (אחרי "פיקוח").
- הסדר החדש: שילוט → רווחה → הנדסה → חניה → פיקוח → **רוכשים בקליק** → חינוך → טפסים דיגיטליים → ארנונה (9 פריטים במקום 10).
- `classes.svg` נשאר ב-`public/icons/` למקרה שיוחזר בעתיד.

### נוסף ✨ — סשן 2026-06-21 (אייקון "רוכשים בקליק" — חנות אונליין)
- נוסף `/icons/online-shop.svg` חדש בסגנון תואם לסט (קווי מתאר `#027DB3`, מילויים `#83DFE8`/`#5CEBBE`):
  - חזית חנות (storefront) עם סוכך פסים מקושת בתחתית
  - שני חלונות ראווה משני צידי הדלת
  - דלת ראשית מקושתת עם ידית
  - סמן עכבר/קליק לבן עם outline כחול — מסמל "קליק" לרכישה אונליין
- `Home.jsx` — הפריט "רוכשים בקליק" עודכן מ-`clickpay.svg` (אייקון תשלום) ל-`online-shop.svg` (אייקון חנות) שמתאר טוב יותר חיבור לחנות אינטרנטית

### השתנה 🔧 — סשן 2026-06-19 (סבב 15 — Hero נקי וסטטי לפי 111.jpg)
- בוטלה אנימציית התלת-ממד של המשי (סבב 10): `perspective`, `::before`, `rotateX/Y/Z`, `translate3d`, `scale`, `silk-3d-camera`, `silk-3d-pulse`.
- ה-Hero חזר לרקע פשוט: `background: #0a3a55 url('/hero-bg.png') center / cover no-repeat;` - תמונת המשי הסטטית הברורה, כפי שמופיע בעיצוב הרפרנס `111.jpg`.
- שכבות `.wave-a/.wave-b` המסתובבות נשארות (הוסיפו אנימציה רכה ברקע).

### השתנה 🔧 — סשן 2026-06-19 (סבב 14 — הסרת רצועת הגלים לפי 222.jpg)
- הוסרה לחלוטין `.hero-waves` (4 שכבות SVG + CSS animations) - לא הופיעה בעיצוב הרפרנס `_design/222.jpg`.
- ה-Hero חוזר למעבר נקי לשורת האייקונים הלבנה.
- האנימציה התלת-ממדית של רקע המשי (סבב 10) נשמרת.

### תוקן 🐛 — סשן 2026-06-19 (סבב 13 — אזור הטורקיז התקטן)
- המשתמש דיווח שאזור ה-Hero התקטן ויזואלית. הסיבה: `.hero-waves` בגובה 140px כיסתה חלק מאזור החיפוש.
- גובה הרצועה הוקטן מ-**140px → 55px**, כל 4 שכבות הגלים נשארו פעילות אבל דקות יותר.
- (סבב 14 הסיר אותן לגמרי לפי בקשת המשתמש.)

### נוסף 🚀 — סשן 2026-06-19 (Deployment)
- האתר עלה ל-Cloudflare Workers: **https://muni2026v1.choreshchana.workers.dev/**
- חיבור בוצע ע"י `npx wrangler login` (OAuth).

### נוסף ✨ — סשן 2026-06-19 (סבב 12 — אייקון "טפסים דיגיטליים" עם קישור חיצוני)
- נוסף `/icons/digital-forms.svg` בסגנון תואם (קווי מתאר `#027DB3`, מילויים בהירים `#83DFE8`/`#5CEBBE`, צ'ק ירוק עיגול לבן):
  - דף עם פינה מקופלת, 3 שורות טופס
  - עט/חתימה דיגיטלית בצד עליון
  - סימן וי בעיגול בצד תחתון (אינדיקציית "דיגיטלי")
- הפריט נוסף ל-`services` array בין "חינוך" ל-"ארנונה", עם `href: 'https://epr-digital-public.pages.dev/'`

### נוסף ✨ — סשן 2026-06-19 (סבב 11 — לחיצה על חינוך מנווטת לאתר אחר)
- אייקון **חינוך** בשורת השירותים העליונה הפך לקישור ל-`https://departmentof.choreshchana.workers.dev/`
- בוצע ע"י הוספת `href` לאובייקט השירות; ה-rendering המותנה ב-`s.href` מחליף `<button>` ב-`<a>`
- שאר האייקונים נשארו כפתורים (ללא ניווט)

### השתנה 🔧 — סשן 2026-06-19 (סבב 10 — רקע המשי כסצנה תלת-ממדית חיה)
- **הקפלים הטורקיזיים עכשיו נעים בתלת-ממד אמיתי** כמו צילום סרט סינמטי:
  - `perspective: 1400px` על `.hero-bg` → מרחב תלת-ממדי
  - שכבת המשי על `::before` עם `transform-style: preserve-3d`
  - `silk-3d-camera` 22s ease-in-out alternate: `rotateX/Y/Z` (עד 7°) + `translate3d` עם Z עומק (60px) + `scale 1.04→1.12`
  - `silk-3d-pulse` 9s: brightness+saturate+contrast → המשי "נושם"
  - `inset: -12%` כדי שהסיבוב לא יחשוף קצוות
- שכבות הגלים בתחתית (סבב 9) נשארו פעילות

### השתנה 🔧 — סשן 2026-06-19 (סבב 9 — שכבות SVG גלים בתחתית ה-Hero לפי אפיון מדויק)
- **אפיון המשתמש**: גלים חופפים בתחתית ה-Hero, שכבות עם שקיפויות שונות, אנימציית CSS על ציר X במהירויות שונות, לולאה אינסופית.
- בוטל SVG filter (silk-wave) מסבב 8 — חזרה ל-`hero-bg` נקי עם תמונת המשי הסטטית.
- נוסף `.hero-waves` — מיכל בתחתית ה-Hero עם 4 שכבות SVG `<path>` חופפות:
  - **Layer 1**: `#83DFE8` opacity 0.40, **28s** שמאלה (period 720, 4 גלים)
  - **Layer 2**: `#5CEBBE` opacity 0.55, **22s** ימינה (period 480, 6 גלים)
  - **Layer 3**: `#27B7C9` opacity 0.75, **16s** שמאלה (period 360, 8 גלים)
  - **Layer 4**: `#FFFFFF` opacity 1, **11s** ימינה (period 288, 10 גלים) — מתחבר לרקע הלבן של האייקונים
- **תקופת גל מתחלקת ב-1440** (חצי viewBox) → `translateX(-50%)` יוצר לולאה seamless ללא קפיצה
- כל path בנוי עם `T` (smooth quadratic) ליצירת גלים זורמים סינוסואידיים נקיים

### השתנה 🔧 — סשן 2026-06-19 (סבב 8 — קפלי המשי מתעוותים כמו גלי מים)
- **הקפלים עצמם מתעוותים** (לא רק התמונה זזה) — המשתמש הבהיר שצריך אדוות אמיתיות:
  - **SVG filter ב-App.jsx**: `feTurbulence` (fractalNoise) + `feDisplacementMap` עם `scale="40"` → מעוות פיקסלים בפועל
  - `<animate>` SMIL על `baseFrequency` ב-14s → 4 ערכים שונים → הדפוס משתנה כל הזמן → אפקט אדוות חי
  - הפילטר מוחל על `.hero-bg::before` בלבד (תמונת המשי), כך שהטקסט וסרגל החיפוש נשארים חדים
  - `inset: -8%` כדי שהעיוות לא יחשוף קצוות
- בוטלה אנימציית `background-position` הסטטית מסבב 7 - לא נראתה כתנועת גלים

### השתנה 🔧 — סשן 2026-06-19 (סבב 7 — קפלי ה-Hero זזים כמו וידאו חי)
- **המשתמש הבהיר עם 333.jpg** — הוא התכוון לאלמנט קפלי המשי של **רקע ה-Hero** (`hero-bg.png`), לא לאזור הכרטיסים. בוטלו השינויים ב-`cards-bg` (חזרה לבלובים העדינים המקוריים).
- **אנימציה ב-Hero**:
  - `background-size: 180-190%` (התמונה גדולה מהמיכל → יש מרחב להזזה בלי להראות קצה)
  - `background-position` נע מ-`0% 45%` → `50% 55%` → `100% 45%` ב-20s ease-in-out alternate → הקפלים זורמים אופקית עם תזוזה אנכית קלה
  - `filter` עם `brightness`+`saturate`+`contrast` ב-9s → "נשימה" חיה של האור על המשי
  - `overflow: hidden` למניעת הצצה של גלילות הגלים
- שני הגלים האנימטיביים (`.wave-a/.wave-b`) המסתובבים נשארים מעל הקפלים → שכבת רכות נוספת

### השתנה 🔧 — סשן 2026-06-19 (סבב 6 — קפלי משי תלת-ממדיים זורמים כמסוע)
- **קפלי משי טורקיזיים כמו hero-bg** — המשתמש התכוון לאפקט הקפלים התלת-ממדיים שיש בתמונת `hero-bg.png`, ושהם יזוזו אופקית כמו מסוע נע.
- 3 שכבות `.silk-fold` עם דפוס SVG tileable של 400px:
  - גרדיאנט אופקי של 8 stops בטונים שונים (`#027DB3`, `#83DFE8`, `#5CEBBE`, `#27B7C9`) מדמה bands של אור/צל
  - paths לבנים/שחורים שקופים מדמים highlights/shadows של קפלים תלת-ממדיים
  - `feGaussianBlur` רך לתחושת בד משי
- **תנועת מסוע חלקה (seamless conveyor)** — `translateX(-200px)` שזה בדיוק tile אחד, כך שאין קפיצה בלולאה:
  - `silk-fold-top` (18s, שמאלה)
  - `silk-fold-mid` (14s, שמאלה) — רצועת highlight אופקית באמצע
  - `silk-fold-bottom` (24s, ימינה) — לאפקט parallax הפוך
- drop-shadows חזקים (opacity 0.45) לעומק תלת-ממדי

### השתנה 🔧 — סשן 2026-06-19 (סבב 5 — גלים זורמים אופקית כמו סרט וידאו)
- **גלים זורמים אמיתיים** — הוחלף `wave-float` (תזוזה קלה הלוך-חזור) ב-`flow-left/flow-right` עם `translateX(0)→translateX(-50%)` ב-`linear infinite`. כל שכבה היא רוחב 200% עם `background-repeat: repeat-x` ו-`background-size: 50% 100%`, כך שהדפוס זורם ברצף ללא תפר — בדיוק כמו וידאו של גלי ים שזורמים.
- **5 שכבות גלים** עם מהירויות וכיוונים שונים ליצירת אפקט parallax:
  - `flow-wave-top` (14s, שמאלה) — גל טורקיז עז למעלה
  - `flow-wave-top-2` (22s, ימינה) — שכבה שנייה מאחור לעומק
  - `flow-wave-mid` (28s, שמאלה) — קו זורם דרך אזור הכרטיסים
  - `flow-wave-bottom-2` (26s, שמאלה) — שכבה תחתונה עמוקה
  - `flow-wave-bottom` (18s, ימינה) — גל teal מלמטה
- כל הגלים עם `drop-shadow` תלת-ממדי לעומק, blur קל לאפקט מים

### השתנה 🔧 — סשן 2026-06-19 (סבב 4 — גלי טורקיז מודגשים תלת-ממדיים)
- **גלים טורקיזיים מודגשים מאוד ברקע הכרטיסים (לפי 222.mp4)** — הוחלף הרקע העדין `cards-bg` (opacity 0.18-0.22) באפקט תלת-מימדי חזק:
  - 3 שכבות `radial-gradient` עזות בטורקיז (#83DFE8, #5CEBBE, #2DB6E0) עם opacity 0.35-0.55
  - גל SVG עליון (`::before`) עם `linearGradient` טורקיז→ירקרק + `feDropShadow` + `drop-shadow` חיצוני לעומק תלת-ממדי
  - גל SVG תחתון (`::after`) בכיוון הפוך עם הילה תלת-ממדית
  - שכבת גל אמצעית (`cards-wave-mid`) — קו טורקיז זורם רחב (stroke-width 80) עם blur ו-drop-shadow
  - אנימציות `wave-float-a/b` 12s/15s/18s לתנועה רכה ותחושה חיה
  - הכרטיסים עברו ל-`z-10` כדי לרחף מעל הגלים

### תוקן 🐛 — סשן 2026-06-19 (סבב 3)
- **גלים עדיין סטטיים** — הסיבה: `ease-in-out alternate` יוצר עצירות מורגשות בנקודות הקיצון, נראה כסטטי. הוחלף ב-**`linear infinite` עם סיבוב 360° רציף** של שני שכבות מסתובבות בכיווני נגד (16s + 22s), + scale pulsing 1.0↔1.15. עכשיו זרימה חלקה כמו גלי ים.

### תוקן 🐛 — סשן 2026-06-19 (סבב 2)
- **גלים לא זזו**: הוסר `mix-blend-mode: screen` (היה רך מדי); הוגבר אופסיטי ל-0.85, אנימציה מהירה יותר (10s/13s), טווח תזוזה רחב יותר ו-scale עד 1.3
- **טקסט הופיע משמאל בכרטיסים**: `justify-end` ב-RTL = end-of-axis = **שמאל**! הוחלף ב-`justify-between` (header) או הוסר כליל ל-`text-right` בלבד עבור פריטים פשוטים

### השתנה 🔧 — סשן 2026-06-19 (Polish לפי 111.jpg + 222.jpg)
- **כיווניות עברית בכרטיסים (RTL)** — הופכת DOM order במקום שימוש בעקיפים:
  - Header של כרטיס: כותרת מימין, אייקון משמאל
  - פריט נפתח: טקסט מימין, חץ `ChevronLeft` משמאל (סיבוב 90° בפתיחה)
- **רקע Hero אנימטיבי 🌊** — תמונת PNG אמיתית מ-Figma + שתי שכבות `radial-gradient` מטושטשות במצב `mix-blend-mode: screen` עם אנימציה אינסופית של תזוזה+סקאלה+סיבוב (14s/18s). מכבד `prefers-reduced-motion`.
- **לוגו muni** — פוצל ל-`muni-wordmark.svg` (האותיות בלבד) + טקסט עברי אמיתי `<span dir="rtl" lang="he">הבית הדיגיטלי שלך</span>` במקום vector paths מטושטשים
- **תיקון preserveAspectRatio** — הסרת `preserveAspectRatio="none"` שגרם למרוח בלוגו
- **רקע ה-Hero** — תמונת PNG אמיתית מ-Figma (אריג טורקיז) במקום CSS gradient
- **פריסה** — שורת האייקונים ירדה מתחת ל-Hero על רקע לבן (לא חופפת); שורת החיפוש מרחפת על השפה
- **User pill** — סדר RTL תוקן: אייקון אדם בימין, חץ-למטה בשמאל

### נוסף ✨ — נכסי Figma אמיתיים
- 9 אייקוני שירות SVG אמיתיים מ-Figma (קווי מתאר עדינים בכחול #027DB3):
  רוכשים בקליק, שילוט, רווחה, הנדסה, חניה, פיקוח, חוגים, חינוך, ארנונה
- 4 אייקוני כותרת כרטיסים: אזור אישי, פעולות מהירות, תשלומים, אישורים
- לוגו מועצה אזורית מנשה (PNG עם החותם הירוק)
- לוגו "muni" אמיתי כולל תת-כותרת "הבית הדיגיטלי שלך"

### השתנה 🔧
- `<ServiceIcon>` הוחלף ב-`<img>` של ה-SVG המקורי (במקום Lucide icons בגרדיאנט)
- `<MenuCard>` מקבל `iconSrc` במקום `icon` (קומפוננטה)
- `<Logo>` משתמש ב-muni.svg עם CSS filter למצב כהה

### השתנה 🔧 — נאמנות ל-Figma
- **פונט** Heebo → **Rubik** (כל האתר), בהתאם למקור
- **צבעים** עודכנו לפי tokens מ-Figma:
  - Primary `#027DB3` (היה `#1F87C7`)
  - Secondary `#83DFE8`, Accent `#5CEBBE`, Light `#E6F2F7`
  - Gray6 `#625F68`, Dark gray `#484848`
- **Hero** עוצב מחדש עם רקע גרדיאנט תכלת-טורקיז ובלובים רכים
- **שורת חיפוש** 848×72 pill צף, צל כחול `0 16px 16px rgba(2,125,179,0.2)`
- **שורת אייקונים** — 9 אייקונים עגולים (100×100) עם גרדיאנט טורקיז+חצי גלילה
- **כרטיסים** — 4 כרטיסים (500px) רחבים rounded-[36px], header עם אייקון 70×70 וכותרת 28px
- כל פריט תפריט בכרטיס (46×46), עם תמיכה ב-expandable items
- **Header** — לוגו מועצה 100px מצד ימין, user pill, כל השירותים + muni logo משמאל
- **Footer** — watermark "muni" + צללית עיר טורקיז, EPR + TSG כתגיות
- **Login** — רקע hero, כרטיס לבן rounded-[36px], טיפוגרפיה מותאמת

### נוסף ✨
- רכיב `MenuCard` — כרטיס תפריט עם פריטים נפתחים (expandable)
- רכיב `ServiceIcon` — אייקון שירות עגול מתעופף ב-hover
- שדרוג Vite 5.4 → 6.4 (תאימות Cloudflare Workers)
- `wrangler.jsonc` — `not_found_handling: single-page-application`

### הוסר 🗑️
- `public/_redirects` — לא נתמך ב-Cloudflare Workers
- פונט Heebo (הוחלף ב-Rubik)
- אייקון "חוגים" (`classes.svg`) משורת השירותים — נשמר ב-`public/icons/` להחזרה אפשרית
- `online-shop.svg` — אייקון חנות-פיזית; הוחלף ב-`mobile-pay.svg`

---

## [0.1.0] — 2026-06-19

### נוסף ✨
- **תשתית פרויקט** — Vite + React 18 + Tailwind CSS 3
- **RTL + Hebrew** — עברית מלאה, פונט Heebo
- **Routing** — React Router 6 עם Layout משותף
- **דף הבית** (`/`):
  - Hero עם רקע גרדיאנט כחול-טורקיז
  - שורת חיפוש בולטת
  - 9 אייקוני שירותים (ארנונה, חינוך, רווחה, הנדסה, וכו')
  - 4 כרטיסי דשבורד: אזור אישי, פעולות מהירות, תשלומים, אישורים
  - סקציית "אודות" עם רקע על EPR/TSG
- **התחברות** (`/login`):
  - שלב 1: ת.ז (9 ספרות) + סיסמה + הצג/הסתר סיסמה
  - שלב 2: OTP בן 6 ספרות עם autofocus
  - ניתוב אוטומטי לאזור האישי לאחר אימות
- **אזור אישי** (`/my-account`):
  - באנר ברוכים הבאים
  - תפריט צד עם 6 קטגוריות
  - סקירה כללית: סטטיסטיקות + פעילות אחרונה
  - נכסים שלי / היסטוריית תשלומים / בקשות / הודעות / הגדרות
- **תשלומים** (`/payments`):
  - בחירת חיובים פתוחים
  - חישוב סכום דינמי
  - סיכום תשלום עם תקן PCI-DSS
- **טפסים** (`/forms`):
  - חיפוש חופשי
  - סינון לפי 6 קטגוריות
  - מילוי מקוון או הורדה
- **שירותי רשות** (`/service/:slug`):
  - 9 קטגוריות: arnona, education, revaha, handasa, forms, events, transport, environment, security
  - לכל קטגוריה: באנר + פעולות מרכזיות
- **דף 404** — עיצוב נקי לדפים לא קיימים
- **Header** — לוגו MUNI, ניווט ראשי, התראות, כניסה, בורר רשות
- **Footer** — צללית עיר, 4 טורים (שירותים/מידע/קשר/חברה), קרדיט EPR + TSG
- **רכיבי Tailwind** — `.btn-primary`, `.btn-outline`, `.card`, `.input`, `.chip`
- **פלטת צבעים** — 8 גוונים של muni (deep/dark/primary/accent/light/bg/text/muted)
- **תיעוד** — README מלא + CHANGELOG

### בסיס עיצובי 🎨
- מבוסס על Figma: `MUNI / Final Designs - Frozen`
- מסכים מהמקור: `DT HOME`, `DT_Log-Ins`, `DT_MY Area`, `DT_Forms`, `DT_MAIN`

### תשתית 🛠
- ESM modules (`"type": "module"`)
- Tailwind config עם theme מותאם (colors, fonts, shadows, gradients)
- Vite config עם פתיחה אוטומטית של דפדפן
- `.gitignore` סטנדרטי
- Favicon SVG ייעודי

---

## פורמט הרשומות

```
## [גרסה] — YYYY-MM-DD

### נוסף ✨
- תכונות חדשות

### השתנה 🔧
- שינויים בתכונות קיימות

### תוקן 🐛
- תיקוני באגים

### הוסר 🗑️
- תכונות שהוסרו

### אבטחה 🔒
- שיפורי אבטחה
```
